apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: fraud-scoring
  namespace: fraud-scoring
spec:
  interval: 5m
  chartRef:
    kind: OCIRepository
    name: ml-charts
    namespace: flux-system
  values:
    image:
      repository: ghcr.io/davidpad18/fraud-scoring
      tag: latest # {"$imagepolicy": "flux-system:fraud-scoring:tag"}